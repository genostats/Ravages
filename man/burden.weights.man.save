\name{burden.weights}
\alias{burden.weights}
\title{
  Weights of variants for burden
}
\description{
  Computes weights of variants as performed in SKAT in the liability model
}
\usage{
  burden.weights(haplos, maf.threshold = 0.01)
}
\arguments{
  \item{haplos}{A matrix of haplotypes with one row per haplotype and one column per variant}
  \item{maf.threshold}{ The 


burden.weights <- function(haplos, threshold = 0.01) {
  fr <- colMeans(haplos)
  we <- -0.4*log10(fr)  # le 0.4 pourrait disparaÃ®tre puisqu'aprÃ¨s on remet Ã  l'Ã©chelle
  # on met Ã  0 les SNPs monomorphes et ceux qui sont trop frÃ©quents
  we[ fr == 0 | fr > threshold ] <- 0
  we
}

